#!/bin/bash

rm -f sample/*.txt sample/*.xml
FILES=`ls sample`
FORMATS="txt xml"

echo $FILES
for FILE in $FILES
do
	for FORMAT in $FORMATS
	do
		DST=sample/$FILE.$FORMAT
		echo "sample/$FILE -> $DST"
		elf2txt sample/$FILE -f $FORMAT -o $DST
	done
done
